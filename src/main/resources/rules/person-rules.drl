package rules

import example.droolsdemo.model.Person

// 规则文件示例：判断是否为成年人
rule "Mark as Adult"
    when
        $p : Person( age > 18)
    then
        $p.setAdult(true);
        $p.setAge(18);
        System.out.println($p.getName() + " is an adult!");
end